# Unreleased

## Changed

- 同步 `.devcodex/flex-rate-limit/profile/` 与公共契约面，明确包元信息、公开接口、类型和文档入口变更必须联动回写 profile。
- 修正文档与类型头部中的版本漂移：`docs/README.md`、`website/rspress.config.ts`、`index.d.ts` 统一对齐 `package.json` 的 `2.0.2`。
